这是一个C语言实现的mips汇编器,支持mips.asm中出现的所有指令,有语法检查功能。

大致实现思路：
1. 忽略所有注释和标点符号(包括偏移量和基址间的括号)
2. 余下的内容被分成一个个token数组,类型为指令,立即数,标签,寄存器之一
3. 构造token结构体,每个结构体能表示并储存以上四种类型中的任意一个的所有信息
4. 遍历所有token,遇到标签就记录地址,遇到指令就按指令格式依次读取操作数,其他情况就属于语法错误
5. 每条标签和指令序列都被当作指令看待,翻译为机器码
6. 结果写入文件


一开始没想过会这么复杂,就没有好好规划结构,最终导致所有东西挤在一个文件里
没学过编译原理也不知道怎么分词,总之能用就行

注: 
1. 文本划分为token的方法来自github上的acwj项目的scanner部分,很久之前兴趣使然看了一点没想到能用上
2. 使用方法："mips.asm" | ./mips ,或者./mips后手动输入文件名
3. 可能有bug,依稀记得是标签处理部分
